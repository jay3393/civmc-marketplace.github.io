// Flip to true and redeploy to hard-lock all routes to /waitlist
export const LOCK_TO_WAITLIST_FLAG = false;

// Set to true if you want the lock to apply locally (localhost) as well
export const LOCK_INCLUDE_LOCAL = false; 